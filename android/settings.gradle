// settings.gradle

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
       
        maven { url "https://transistorsoft.github.io/react-native-background-fetch/repo" }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    
        maven { url "https://transistorsoft.github.io/react-native-background-fetch/repo" }
    }
}

rootProject.name = "android"
include(":app")
include(":react-native-background-fetch")
project(':react-native-background-fetch').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-background-fetch/android')