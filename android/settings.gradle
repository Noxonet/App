pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "android"
include ':app'
include ':react-native-background-fetch' // این خط را اضافه کنید.
includeBuild("../node_modules/@react-native/gradle-plugin")