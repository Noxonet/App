buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // فقط React Native gradle plugin
        classpath("com.facebook.react:react-native-gradle-plugin")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
